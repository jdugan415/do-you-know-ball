name: do_you_know_ball
description: A ten-player NFL roster quiz, starting with Pittsburgh.
publish_to: none
version: 0.3.0+3
environment:
  sdk: ^3.13.2
dependencies:
  http: ^1.6.0
  flutter:
    sdk: flutter
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
flutter:
  uses-material-design: true
  assets:
    - assets/teams.json
    - assets/rosters/
